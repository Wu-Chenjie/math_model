===== PAGE 19 =====
A
支撑材料文件列表
文件/目录
内容
code/
全部源程序（见附录
B；Python
3.11，依赖
numpy/scipy/pandas/openpyxl/matplotlib）
data/data.npz
预处理后的全年数据（负载、光伏、电价、预报）
results/result1.xlsx
问题1 结果（计划购电量、充放电量）
results/result2.xlsx
问题2 结果（计划购电量、充放电量、紧急购电量）
results/result3.xlsx
问题3 结果（计划/调整购电量、充放电量、紧急购电量）
results/result4-2.xlsx
问题4 问题2 口径结果（波动电价）
results/result4-3.xlsx
问题4 问题3 口径结果（波动电价）
results/paper_tables.json
论文全部表格数值的机器可读汇总
results/p1--p4_*.npz
各问题逐日求解中间结果
figures/fig1--fig5.png
论文图1–5（300 dpi）
表16
支撑材料清单（赛题原始附件为公开数据，未重复收录）
运行方式：在code/ 目录执行python run_all.py，依次完成数据预处理、四问求
解、确定性基线、结果工作簿生成、表格导出、图形生成与独立校验，全程约6–8 分钟；
单独重跑某一步可直接执行对应脚本。
附件5 模板时段标签勘误说明：附件5 模板的时段标签自第2 数据行起为“0:00–
0:10”，首行缺失该标签、末行多出“0:00+1–0:10+1”，整体错位一格。本文按位置对齐
填充：第1–144 数据行对应“0:00–0:10” ∼“23:50–0:00(+1)”，结果文件中的标签已修正
为规范序列。
B
源程序代码
以下为全部核心源程序（完整可运行，另含总控脚本run_all.py；Python 3.11 环
境实测通过，全部结果可一键复现）。
B.1 common.py——常数、数据加载与单日确定性LP
1
# -*- coding: utf-8 -*-
2
"""公共模块：数据加载、单日计划LP、执行仿真、校验。
3
4
模型记号（每时段t=0..143，Δt=1/6 h）：
5
p_t 购电量(kWh)
c_t 充电量(kWh，储能侧充入前)
6
d_t 放电量(kWh，储能侧放出) S_t 时段末储能量(kWh)
7
充放电效率eta=0.9：充电ΔS=0.9·c_t；放电ΔS=−d_t，输出到交流侧0.9·d_t
8
供电-需求平衡(￿)：p_t + PV·Δt + 0.9·d_t ￿L·Δt + c_t （多余弃光，不售电）
9
"""
19

===== PAGE 20 =====
10
import numpy as np
11
from scipy.optimize import linprog
12
13
DT = 1.0 / 6.0
14
N = 144
15
CD_MAX = 5000.0 * DT
# 单时段最大充/放电量833.333 kWh
16
SOC_MIN, SOC_MAX = 1200.0, 10800.0
17
ETA = 0.9
18
19
_DATA = None
20
21
def load_data():
22
global _DATA
23
if _DATA is None:
24
z = np.load(r"D:/shumo/shumoC/data/data.npz")
25
_DATA = {k: z[k] for k in z.files}
26
return _DATA
27
28
# ---------------- 单日计划LP ----------------
29
30
def solve_day_lp(price, load_hat, pv_hat, s_init, s_end):
31
"""单日计划LP：min Σ ￿_t·p_t
32
price/load_hat/pv_hat: (144,) 电价(元/kWh)、预测负载(kW)、预测光伏(kW)
33
s_init: 日初储电量; s_end: 日末储电量(None=自由，仅受上下限约束)
34
返回dict(p,c,d,S) 各(144,)，S 为时段末储电量轨迹(含时段0)。
35
"""
36
n = N
37
# 变量: p(0..143) c(144..287) d(288..431) S(432..575=S_1..S_144)
38
nv = 4 * n
39
c_obj = np.zeros(nv)
40
c_obj[:n] = price
41
42
# 平衡: -p -0.9d + c <= PV·Δt - L·Δt
43
A_ub = np.zeros((n, nv)); b_ub = np.zeros(n)
44
idx = np.arange(n)
45
A_ub[idx, idx] = -1.0
# -p
46
A_ub[idx, 2*n + idx] = -ETA
# -0.9 d
47
A_ub[idx, n + idx] = 1.0
# +c
48
b_ub = pv_hat * DT - load_hat * DT
49
50
# SOC递推: S_t - S_{t-1} - 0.9c_t + d_t = 0, S_0=s_init
51
A_eq = np.zeros((n, nv)); b_eq = np.zeros(n)
52
A_eq[idx, 3*n + idx] = 1.0
# S_t (t=0 →S_1)
53
if n > 1:
54
A_eq[idx[1:], 3*n + idx[:-1]] = -1.0 # -S_{t-1}
55
A_eq[idx, n + idx] = -ETA
# -0.9 c
56
A_eq[idx, 2*n + idx] = 1.0
# +d
57
b_eq[0] = ETA * 0.0 + s_init * 1.0
# S_1 = s_init + 0.9c - d →S_1-0.9c+d = s_init
58
59
bounds = ([(0, None)] * n + [(0, CD_MAX)] * n + [(0, CD_
===== PAGE 21 =====
66
bounds=bounds, method='highs')
67
if not res.success:
68
raise RuntimeError('LP failed: ' + res.message)
69
x = res.x
70
p, c, d, S = x[:n], x[n:2*n], x[2*n:3*n], x[3*n:]
71
# 清洗数值噪声
72
p[np.abs(p) < 1e-9] = 0; c[np.abs(c) < 1e-9] = 0; d[np.abs(d) < 1e-9] = 0
73
return dict(p=p, c=c, d=d, S=S, cost=float(price @ p))
74
75
# ---------------- 执行仿真----------------
76
77
def simulate_day(p, c, d, load_act, pv_act, price, emergency_mult=5.0):
78
"""按给定购电/充放电轨迹在实际负载/光伏下执行一天。
79
缺口= 负载+ 充电需求−购电−光伏−放电输出，若>0则紧急购电(emergency_mult倍价)。
80
返回紧急购电量向量与费用分解。
81
"""
82
supply = p + pv_act * DT + ETA * d
83
demand = load_act * DT + c
84
gap = demand - supply
85
e = np.maximum(gap, 0.0)
# 紧急购电量(kWh)
86
cost_plan = float(price @ p)
87
cost_em = float(emergency_mult * price @ e)
88
return dict(e=e, cost_plan=cost_plan, cost_em=cost_em,
89
cost_total=cost_plan + cost_em, soc_end=None)
90
91
def soc_track(s_init, c, d):
92
"""由充放电轨迹计算SOC轨迹（时段末，144个）。"""
93
return s_init + np.cumsum(ETA * c - d)
94
95
def check_day(S, c, d, e=None, load_act=None, pv_act=None, p=None):
96
"""校验SOC约束与功率约束，返回违例列表。"""
97
errs = []
98
if S.min() < SOC_MIN - 1e-6: errs.append(f'SOC min {S.min():.2f} < {SOC_MIN}')
99
if S.max() > SOC_MAX + 1e-6: errs.append(f'SOC max {S.max():.2f} > {SOC_MAX}')
100
if c.max() > CD_MAX + 1e-6: errs.append(f'charge {c.max():.2f} > {CD_MAX:.2f}')
101
if d.max() > CD_MAX + 1e-6: errs.append(f'discharge {d.max():.2f} > {CD_MAX:.2f}')
102
return errs
103
104
# ---------------- 表格输出辅助----------
===== PAGE 22 =====
5
负载
L̂ [d] = L[d-7]（上周同期；周周期强，MAPE 3.96% vs naive 15.97%）
6
光伏P2/P4-2: P[d-1]（naive, MAPE 7.7%）；P3/P4-3: 附件3预报（corr￿0.99）
7
场景（SAA）：最近K天"实际−预测"误差样本叠加到当前预测，截断非负。
8
9
计划LP（0:00，两阶段）：
10
min Σ ￿p + ￿Σ_k w_k Σ ￿e^k
￿=5（紧急倍率）
11
s.t. p + P^k Δt + 0.9 d + e^k ￿L^k Δt + c ￿k (第一/二阶段解耦)
12
S_t = S_{t-1} + 0.9c_t −d_t, 1200￿S￿10800, c,d￿833.33
13
S(24:00)=S(0:00)=6000（每日完整循环）
14
调整LP（6/12/18时，从预报可覆盖首时段t0起）：
15
min Σ_{t￿t0} [￿p_t + 0.5￿u_t] + 5 Σ w_k Σ ￿e^k, u_t￿|p_t−p_plan,t|
16
费用（P3/P4-3）：总= Σ￿p_final + 0.5Σ￿|p_final−p_plan| + 5Σ￿E_emerg
17
执行（实时反馈）：合同购电量按计划/调整执行；电池观测当期实际净负荷优先
18
吸收偏差（富余充电/短缺放电），日循环方案（P2/P3）经终端可达区间投影
19
保证日末=6000；两日滚动方案（P4）不投影，窗口末目标由次日滚动重解承接；
20
剩余缺口→紧急购电（5倍价），富余→弃光（免费）。
21
"""
22
import sys
23
import numpy as np
24
from scipy.optimize import linprog
25
from scipy.sparse import coo_matrix
26
27
sys.path.insert(0, r'D:/shumo/shumoC/code')
28
from common import (load_data, solve_day_lp, soc_track, DT, N,
29
CD_MAX, SOC_MIN, SOC_MAX, ETA)
30
31
ADJUST = [(1, 42), (2, 78), (3, 114)] # (预报slot, 该slot预报可覆盖的首时段)
32
EM_MULT = 5.0
33
DEV_MULT = 0.5
34
35
def merge_emerg_segments(e):
36
"""紧急购电量向量→[(start_t, end_t_excl, qty)] 连续段。"""
37
segs, t = [], 0
38
on = e > 1e-9
39
while t < N:
40
if on[t]:
41
t2 = t
42
while t2 < N and on[t2]:
43
t2 += 1
44
segs.append((t, t2, float(e[t:t2].sum())))
45
t = t2
46
else:
47
t += 1
48
return segs
49
50
def hhmm(t):
51
m = int(t * 10)
52
return f'{m//60:02d}:{m%60:02d}'
53
54
# ---------------- 预测与场景----------------
55
56
def pv_fc3(d):
5
===== PAGE 46 =====
28
pv = pd.read_excel(xl2, sheet_name=1).iloc[:, 1:].to_numpy(float)
29
assert load.shape == (365, N) and pv.shape == (365, N)
30
31
# ---------- 附件3：光伏预报----------
32
df3 = pd.read_excel(os.path.join(ATT, '附件3.xlsx'))
33
df3.columns = ['date', 'fc_time'] + [f'h{i}' for i in range(1, 25)]
34
df3['date'] = df3['date'].ffill()
35
# fc_hours[d, s, k]: 第d天(0-based)、第s次发布(0=0:00,1=6:00,2=12:00,3=18:00)、
36
# 预报k小时(1..24) -> 整点(￿+k) mod 24 的功率预报
37
fc_order = {'0:00': 0, '6:00': 1, '12:00': 2, '18:00': 3}
38
df3['slot'] = df3['fc_time'].map(fc_order)
39
fc_hours = np.full((365, 4, 24), np.nan)
40
for _, r in df3.iterrows():
41
d = pd.to_datetime(r['date']).dayofyear - 1
42
s = int(r['slot'])
43
fc_hours[d, s, :] = r[[f'h{i}' for i in range(1, 25)]].to_numpy(float)
44
assert not np.isnan(fc_hours).any()
45
46
# ---------- 附件4：全年波动电价(365,144) ----------
47
price4 = pd.read_excel(os.path.join(ATT, '附件4.xlsx')).iloc[:, 1:].to_numpy(float)
48
assert price4.shape == (365, N)
49
50
np.savez_compressed(
51
os.path.join(OUT, 'data.npz'),
52
price1=a1['price1'], load1=a1['load1'], pv_fc1=a1['pv_fc1'],
53
load=load, pv=pv, fc_hours=fc_hours, price4=price4, dt=DT,
54
)
55
print('saved:', os.path.join(OUT, 'data.npz'))
56
print('load shape', load.shape, 'pv shape', pv.shape, 'fc shape', fc_hours.shape, 'price4 shape', price4.
shape)
C
人工智能工具使用说明
按《全国大学生数学建模竞赛人工智能工具使用规定（2026 年试行）》要求说明：本
研究在程序编写、代码调试与论文文字整理过程中使用了大语言模型辅助工具；模型构
建思路、公式推导、参数选择与结论解释由参赛队完成并逐一验证，全部求解程序、中
间结果与结果工作簿可通过附录B 代码与支撑材料独立复现，参赛队对
