# Seat C：第一次定向修复复核

本次只核验 C-2、C-3、C-4 及新补 Q2 双点下界推导，不重新评分，不读取其他席位意见，不改变首次 scorecard。当前交付根为 `/Users/wuchenjie/Downloads/CUMCM2026Problems/B题几何策略优化`；`work/geometric-20260913` 是评审快照，唯一运行入口是交付根 `run_all.py`。

**结论：C-2、C-3、C-4 修复通过；Q2 新推导通过核验。C-1 官方测试与原始日志缺失继续未解决。** 这只关闭指定缺陷，不表示正式竞赛提交已经完整。

## C-2：通过，旧见证与假成功路径已封闭

`tests/test_geometry_revision.cpp:6–7,27–30` 现在要求显式输出文件参数，检查打开和关闭结果，失败时在 PASS 输出之前返回非零。`tests/run_checks.py:15–22` 删除旧见证，在新临时目录中运行，先验证不存在输出目录时必须失败，再写入明确的绝对路径并验证新文件；记录测试源文件、编译后二进制和见证 SHA-256。`:27–34` 向 Fraction 脚本显式传入同一见证，并要求消费哈希与本次生成哈希一致。

独立验证没有依赖已编译二进制：本评审在新临时目录重新以 `clang++ -O2 -std=c++17` 编译当前测试，然后分别运行三条路径。

| 路径 | 实际结果 |
|---|---|
| 无参数 | exit 2，仅输出使用说明，无 PASS |
| 输出目录不存在 | exit 3，`cannot open witness output`，无 PASS |
| 显式新文件路径 | exit 0，产生本次新见证，4552 个静态叶、4564 个替换叶、3.59481 米替换 |

随后以当前 Fraction 脚本的全部几何与分区逻辑复放这个新文件，21 站、4564 单元、完整圆域分区全部通过，消费哈希为 `573c60ce537641efc0d9e71f31dce6d747646a400eb414656771d8b507da55ae`。为保持评审只读，仅在内存中移除了脚本最后写回交付根结果 JSON 的副作用；验证逻辑未变。

另将现有 `artifacts/regression-tests.json` 中的源文件、二进制、见证哈希与当前真实文件逐一重算比对，三项均一致；`residual-fraction-replay.json` 的消费哈希与上述见证一致。最新完整 `run_all.py` 日志显示通过；本次没有再次重跑全部无关控制器测试。

打包边界也已明确：交付根可运行，工作根仅是审查快照，不要求把 `code/outputs` 快照误当 `src/results/artifacts` 的独立运行包。最终快照三份修复后的测试文件已与交付根逐字节一致。

## C-3：通过，N≥15 的解析连接已补齐

新版 PDF 第 9 页 §6.4、命题 2 将关键链条完整写出：有限站点全朝向接收等价于近邻凸包包含源；两个近邻只能覆盖站点连线上的源，因此除零面积集合外至少三重距离覆盖，积分得 `Σ A_i≥3πR²`。径向向外发射的边界源又要求 `Σ δ_i≥π`。定义每站面积损失 `d_i=1−A_i/(πL²)`，结合逐站 `d_i≥1.38δ_i`，即可推出 `N≥3R²/L²+1.38π>14`，整数性给出 15。

已核对边界半弧公式、两圆交面积积分与 `src/q4_area_arc_certificate.py` 一致；内站、恰在圆周和超过 R+L 的点均已说明。对 r≥R，交面积被积函数随 y 下降，所以左矩形和为上界；使用该上界计算面积损失下界的方向正确。脚本对每个径向段取有效的半弧角上界，因此验证的是整个段的逐站不等式，而非只检查离散半径。

独立重跑脚本输出 `N_real_lower=14.055397861953915`，全部分段余量为正，和论文一致。初次提出的“代码验证单站量但正文未连接到任意站点集”缺口已关闭。

## C-4：通过，已改为准确的浮点证据说明

新版 PDF 第 3 页 §2.2 明确写出：通用几何库返回普通浮点估计，不构成严格向外舍入半径；连续候选另加余量，清除检查使用阈值裕量，全部坐标裁剪误差仍没有形式证明，有理数证书另行标明。这与 `geometry.hpp`、`robust_geometry.hpp` 的实际实现一致。

当前通用几何源文件与冻结快照逐字节相同。修复通过更正陈述完成，没有为了消除评审意见改变冻结算法或重新归因实验收益。原有数值形式化局限仍保留，但不再是论文与代码相互矛盾。

## Q2 新双点下界：通过

新版 PDF 第 4 页给出近共线双点 `(1340,0),(1500,0)`，以及其余区域的旋转射线交点 `B=t u_(e1)`。式 (9) 的 t 分子、分母与有理数脚本一致；将 t≤T 整理后的凹二次式为

`−s‖q‖²+k(1500+T)a+h(1500−T)b−1500Tl≤0`，

正是脚本在包含 G 的圆盘上核验的量。分母正、t>5、两次不会触发近距响应均有独立断言；脚本还检查 `T<1500 cos(2e1)`，所以双点距离在相关 t 区间单调下降，代入上界 T 获得下界的方向正确。

物理量化模型取 `e2<1°`：两方向间隔小于 2°，扩入各自 0.005° 四舍五入余量后，共同可报告角度区间长度大于 0.01°，包含至少一个读数格点；首测端点又保留了避开舍入平局的严格余量。因此没有把仅在连续角度上相交误当作一定存在共同量化读数。

独立重跑 Q2 脚本通过，连续下界 55.73823、物理量化下界 55.6528、候选上界 55.739，208 个上界节点，与新增推导一致。

## 最终 PDF 与快照核对

已查看新版 PDF 第 4、8、9 页渲染图，新增 Q2/Q4 公式完整可读。最终 `paper/solution.pdf` 为 57 页，清理元数据后与交付根 `paper/main.pdf` 的全部版面文本一致；版面文本 SHA-256 为 `8a09a4bde892f6b8ce6b2d8e01c8b32d6328e70f4d56b712e066d3c15d469873`。工作根 `paper-source/main.tex` 与交付根源文件一致。不同 PDF 文件字节哈希来自元数据处理，不是正文内容差异。

本次没有新增评分，也不据以上修复推定评分自动上升。C-1 保持 **blocker（正式提交范围）**：六次官方结果及原始加密日志仍待补齐，论文的本地研究性质和未完成正式验证的披露应继续保留。

```yaml
handoff:
  phase: 7
  agent: mathodology-award-judge
  loop: 1
  status: complete
  artifacts:
    - path: work/geometric-20260913/review/seat-C-fix1.md
      role: targeted-fix-verification
  decisions:
    - "C-2、C-3、C-4 修复通过；Q2 新推导核验通过。"
    - "不重新评分，保留首轮 scorecard。"
    - "C-1 官方日志缺失仍未解决。"
  assumptions:
    - id: FIX1-PACKAGE
      text: "交付根 run_all.py 为唯一运行入口，work 根为评审快照。"
      evidence: "负责人明确界定，快照文件一致性已独立检查。"
      sensitivity_plan: "若后续改为独立发放 work 根运行包，需另验目录布局与入口。"
  evidence:
    - "独立重编译后无参数、无效路径、新见证三分支均符合预期。"
    - "新见证 4564 单元有理数复放通过，哈希链一致。"
    - "Q2、Q4 有理数脚本独立重跑通过。"
    - "57 页最终 PDF 版面文本与交付根 PDF 一致。"
  commands:
    - "python3 B题几何策略优化/src/q2_full_domain_certificate.py /tmp/seat-c-fix1-q2.json"
    - "python3 B题几何策略优化/src/q4_area_arc_certificate.py /tmp/seat-c-fix1-q4.json"
  weaknesses:
    - "C-1：官方正式测试及原始日志仍缺失。"
  questions: []
  critic_focus:
    - "保留正式证据未完成的清晰披露。"
```
