from pathlib import Path
import json
R=Path(__file__).resolve().parents[1];S=json.loads((R/'artifacts/summary.json').read_text());a=S['iid']['3']['vs56'];b=S['iid']['4']['vs56'];c=S['iid']['4']['vs78']
f=lambda x:f'{x:.2f}'
s=r'''# B题几何策略优化题解与计算交接

本轮完成最坏半径选点、最近保证第二测、剩余覆盖认证、真实单源控制器续跑和光学邻域巡游的实现与对照。独立验证尚不能确认新的稳定提速，推荐默认保留方法78；方法84作为经过全清除检验的实验候选单独提供。论文涵盖问题1至4的推导与实验，并附在线代码和几何证书。

## 1 先看最终结果

时间单位均为秒；每问独立200局。

|问题|56均时|84均时|节省|95%区间|慢局|
|---|---:|---:|---:|---|---:|
'''
for q,d in [('Q3',a),('Q4',b)]:s+=f"|{q}|{f(d['baseline_mean_s'])}|{f(d['candidate_mean_s'])}|{f(d['saving_s'])}|[{f(d['ci95_s'][0])}, {f(d['ci95_s'][1])}]|{d['slower']}|\n"
s+=f'''
推荐默认78的独立总均时为Q3 {f(S['iid']['3']['vs78']['baseline_mean_s'])}秒、Q4 {f(S['iid']['4']['vs78']['baseline_mean_s'])}秒。Q3与默认78逐局相同；其{f(a['saving_s'])}秒收益属于旧提前停车，不能算本轮新增。Q4候选相对78节省{f(c['saving_s'])}秒，但95%区间为[{f(c['ci95_s'][0])}, {f(c['ci95_s'][1])}]秒，仍跨零。按冻结规则，候选没有进入默认程序。两问IID和五组压力测试全部清除；未达到新增10%。

这些是本地模拟结果，不是官方正式成绩。目录内没有可核验的Q3、Q4各三次官方正式加密日志，论文保留明确的待填表，不把本地案例冒充正式案例。
'''
s+=r'''
## 2 问题1的完整算法

示向点 $s$、读数 $\theta$ 和半角 $\varepsilon$ 对应两个前向半平面。令 $u=(\cos\theta,\sin\theta)$、$v=(-\sin\theta,\cos\theta)$，约束为

$$|v^T(z-s)|\le \tan\varepsilon\,u^T(z-s).$$

逐条裁剪初始区域，得到凸多边形 $P$。若没有场地先验，先判断半平面交为空、无界、有界或退化；空域报不一致，无界域的直径为无穷。顶点数为 $m$ 时，枚举顶点对以 $O(m^2)$ 求直径，或采用旋转卡壳。

不能以直径不超过40米作为保证清除：边长40米的等边三角形需要半径 $40/\sqrt3=23.094$ 米的圆。正确条件是最小包围圆半径 $\rho(P)\le20$ 米。其支持点至多三个，计算圆心后检查全部顶点距离。

本地读数按0.01度量化，实际实现用1.005度半角；物理误差仍为题设的正负1度。

## 3 问题2及保证区域的空集边界

首测归一化为原点及零度。源 $z=ru_\phi$ 满足 $5<r\le1500$、$|\phi|\le\varepsilon$。首次收到信号隐含 $R_s\ge\max(1000,r)$，完整保证第二次接收域为

$$G=\bigcap_{r\in\{5,1000\},\ \sigma\in\{-1,1\}}B(r(\cos\varepsilon,\sigma\sin\varepsilon),1000).$$

分别用径向凸性和大于1000米后约束的单调性，便可把所有位置约束化到四个端点圆盘。测点 $(843.1178,\pm545.4003)$ 的最坏后验半径上界为55.739米，物理误差加量化模型的全域下界为55.6528米；精确有理数证书本轮已重跑。

因此，对未被场地裁剪的完整首测扇区，要求“一次补测后无论读数如何都保证半径不超过20米或35米”没有可行点。这不是局部优化器没找到。边界裁剪后的 $P$ 另当别论，仍可以搜索最近保证区域；本轮没有证明所有裁剪实例均为空。

在线新局部候选额外使用 $\max_{z\in P}\|q-z\|\le950.7$ 米的充分接收门槛，保持经验点的余量。这比完整保证接收域更小；应避免将它称为全域最优搜索。

## 4 算法1与算法4的实际实现

仅对多边形顶点的无噪声方位切角锥，不能认证最坏剩余半径。实现首先构造所有可能示向区间，并包含正负误差，再分割示向区间。如果一段中心为 $\theta_j$、半宽为 $\delta_j$，则

$$\bigcup_{|\theta-\theta_j|\le\delta_j}W_\varepsilon(q,\theta)\subseteq W_{\varepsilon+\delta_j}(q,\theta_j).$$

对每个加宽角锥与 $P$ 的交求最小包围圆，最大半径是连续读数目标的外包。对最大上界区间递归二分。候选位置使用单纯形优化，不再按粒子分箱和八方向加密。

只保留一个新点，要求其半径改善及行走门槛通过。门槛把直径减少量除以5换成秒数，只是启发式，不能承诺后续实际时间减少。开发40局中，该方法Q3反而平均多用165.79秒，7胜33负。该结果说明半径几何仍需与完成时间衔接。

方法82进一步用原单源控制器从每个候选动作继续执行，保留测量更新、近距清除、剩余动作预算和真正的光学覆盖。最多取4个内点假设及2个可行边界点，并分别取两端误差。平均时间至少减少5秒且有限场景最差退步不超过5秒才替换。实际执行34624次续跑、采纳22次动作，Q3开发均时仅减少1.10秒。

“真实续跑”指调用同一控制器至清除成功。未来观测使用每个场景固定的正负1度误差，不执行0.01度量化，未复用评测的位置相关误差场；只在同位置重放真实历史读数。这是单源控制器的有限场景续跑，未模拟所有其它源的未来联合调度；有限场景的最坏值不等于连续源状态的最坏值。代码没有接入完整POMCPOW，也没有把新候选回灌旧的八方向粒子分。

## 5 算法3的剩余覆盖证明

对Q4，状态必须含位置、半径、朝向及类型。未发现频道的状态集按 $\Omega_k\leftarrow\Omega_k\setminus C(q)$ 更新，不能只裁位置圆盘。若只有一次正常示向，要求换一个新点后对所有合法朝向仍必收，通常只能得到原点本身：对每个可能源位置，新的接收向量必须是旧向量的非负倍数；两条不同源视线同时成立即退化。

实现使用比显式四维状态更容易复核的覆盖不变量。令 $H$ 为已对所有未发现频道完整扫描的点，$F$ 为未来点；对全部 $z\in\Omega$ 要求

$$z\in\operatorname{conv}\{q\in H\cup F:\|q-z\|\le1000\}.$$

任何朝向均至少有一个近邻点在源的正向闭半平面。如果该点全在历史 $H$ 中，源此前就会被发现，故未发现状态必须由未来 $F$ 覆盖。这同时处理所有位置及朝向，不靠粒子接收概率。

空间认证从场地外包正方形开始二分，若一个矩形的所有角点都在公共近邻点的凸包内，且这些点到所有角点均不超过999.999米，就通过。距离平方和叉积以向外舍入区间认证；任一不确定判断、递归上限或预算耗尽都拒绝本次修改。

初始21点使用4552个叶单元通过认证。一个实际接受的点替换用4564个叶单元通过，并由独立Python Fraction核验全部单元和空间分区无缺口。

每个待走发现点尝试向当前点连续移动，只有全域证书仍成立才接受。另实现顺路已停车位置扫描后删点的版本；开发集没有成功删掉整点。负响应只把一个点从 $F$ 移进 $H$，两者并集不变，所以它不会自动把其它必需静态点变冗余。需要新增几何信息或额外测点支撑，不能凭“已经扫过附近”削减全清除保证。

固定点数下界的推导为：除有限条站点连线外，全朝向接收需要至少三个距离1000米内的测点，故 $\sum_i A_i\ge3\pi1800^2$。对边界径向外发射的源，测点可接收边界弧半角 $\delta_i$ 满足 $\sum_i\delta_i\ge\pi$。外部测点的面积损失 $d_i=1-A_i/(\pi1000^2)$ 经有理数区间逐段认证满足 $d_i\ge1.38\delta_i$，内部测点的边界弧为零。因此 $N\ge3(1800/1000)^2+1.38\pi>14$，所以 $N\ge15$。详细弧公式、积分上界和端点处理已列入论文。

本轮固定通用布点下界15已经重新核验，上界21保留；这个静态下界不是局内必能删除6点的承诺。

## 6 算法5的清除邻域与巡游

覆盖子区域 $P_i$ 的合法清除位置是

$$\mathcal C(P_i)=\bigcap_{v\in V(P_i)}B(v,20).$$

若子区域包围圆为 $(c_i,r_i)$，则 $B(c_i,20-r_i)$ 是一个安全内域。不能将原20米覆盖圆直接当成“只要触碰就能清除”的TSPN邻域，否则两端源与机器人可能相距40米。

实现对各子区域保持覆盖证书，交替优化清除点和2-opt顺序，同时尝试可变条带覆盖。只有完整扫掠长度和有限假设的平均首次命中时间都下降才接受。多个已保证近清源也可联合调整访问坐标。它没有求得全部顺序的CETSP全局最优解。

开发集Q4中，原覆盖邻域巡游多用5.21秒，可变覆盖版本多用4.09秒。因此这些候选保留代码和消融，未启用到默认策略。

## 7 开发消融与压力结果

本地普通场景每局有10至16个源。位置在圆内按面积均匀提议，但拒绝源距小于40米的样本；Q4定向源数在1至总数减1之间生成。误差为坐标和种子决定的固定场，随后量化读数。局间独立，局内位置有排斥约束，不能把此均值外推为任意分布的官方均值。

历史CSV在当前平台同种子标签下不逐行相同。本轮重新编译旧的未修改源码，80行开发基线与新版本的方法56完全对齐；所有比较使用本轮同环境重跑的基线，历史数据不混入。


|方法|Q3平均节省/秒|Q4平均节省/秒|用途|
|---|---:|---:|---|
'''
for m in [79,80,81,82,83,85,86,84]:
 d=S['development'][str(m)];s+=f"|{m}|{f(d['3']['saving_s'])}|{f(d['4']['saving_s'])}|{'冻结独立验证候选' if m==84 else '开发消融'}|\n"
s+='\n|压力组，每问40局|Q3平均节省/秒|Q4平均节省/秒|清除|\n|---|---:|---:|---|\n'
for i,name in enumerate(['边界外向','共线端点','近重合','相关误差','分区端点误差'],1):
 d=S['stress'][str(i)];s+=f"|{name}|{f(d['3']['saving_s'])}|{f(d['4']['saving_s'])}|两问均40/40|\n"
s+=r'''
## 8 运行与复现

默认控制器先将未知频道扫描点与已知源下一动作联合排路，只执行首个任务后重算。有限动作树深度2、节点预算6000，每源最多6次局部动作，Q3/Q4假设上限32/63。Q3最多4次机会补测，局部阶段后最多3轮中心试探再进入28米格光学；Q4直接进入自适应条带光学。路由节点不超过12时动态规划精确求解，否则多起点、2-opt和重定位。发现16个源即可停止余下发现，未达16则须完成剩余证书扫描。每个光学子区域都需实际发出清除指令。方法78只把保证成功的清除沿原路径提前，方法84额外允许Q4发现点通过连续证书后移动。

全部操作在解压后的项目根目录执行，目录可以改名。默认方法78，实验候选84使用 `geometric`，对照56使用 `previous`。新会话从原点开始，不能用提前停车适配器中途接管已经移动的机器人。

```bash
python3 run_all.py
python3 run_all.py --full
python3 src/bridge.py --problem 4 --team-id 实际队号 --strategy joint --planner-depth 2
python3 src/bridge.py --problem 4 --team-id 实际队号 --strategy geometric --planner-depth 2
```

`--full`重跑固定独立与压力样本，不重新选参；正式测试须先在官方模拟器界面由队员启动并等待接口就绪。脚本不会自动启动正式测试。`vendor/mocksim` 是所给压缩包内的非官方公开协议实现，不能将其本地HTTP测试称为官方验证。

主要路径：

- `src/robust_geometry.hpp`：连续读数半径外包和局部测点候选。
- `src/residual_cover.hpp`：向外舍入空间认证和发现点替换。
- `src/rollout_sensor.hpp` 与 `policy.hpp`：真实单源控制器续跑。
- `src/neighborhood_tour.hpp`：安全清除邻域与光学序列优化。
- `artifacts/summary.json`：逐局配对与分组统计。
- `validation_frozen.json`：独立集运行前的冻结配置与哈希。
- `artifacts/promotion-decision.json`：置信区间跨零、不推广候选的决定。
- `paper/main.tex`：完整论文，含程序附录。

## 9 文献核对与不能直接移植的结论

谨慎贪心原文采用高斯状态与EKF；Bayram的定位标准是GDOP，并带双准则近似损失。这些论文支持时间意识和先布点后巡游的结构，不提供本题20米角锥半径保证。Sunberg的退化结论针对朴素渐进加宽，POMCPOW恰是其修正算法；不能把它写成POMCPOW普遍退化。割草路径上自动覆盖与本题停车光学指令也有区别。

核验的一手来源：

'''
for x in json.loads((R/'evidence/literature.json').read_text()):s+=f"- {x['authors']}（{x['year']}）[{x['title']}]({x['url']})。\n"
(R/'B题几何策略优化_题解.md').write_text(s)
print('solution markdown created')
