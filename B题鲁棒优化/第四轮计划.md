# 第四轮计划与证据口径

保留三版冻结成果。优先获取固定点数下界和正扰动裕量；少点搜索只在限定计算内探索，不保证发现18—20点，也不把失败搜索当作不存在证明。

Q2在线消融固定三个几何点：A=(750,300)，B=(850,400)，C=(843.1178,545.4003)，分别检验仅Q3替换和两问都替换；不把纯几何精度证书当时间收益。

路线新增可验证优化器：小待办集合用Held–Karp精确开放路径DP；大集合用完整代价矩阵局部搜索，并记录松弛下界、上界与局部终止检查。另比较入口—出口服务代价模型，不再只以最小圆心作点代理。由于未来观测未知，这些证书只针对当前代理问题。

加入“已经发现16个不同频道”时停止全局补漏的有效信息规则；最终退出仍须16个成功清除。该规则依据题目数量上界，不读取真值。

开发种子1—40，只选时间控制器，全部候选结果保留。冻结后用新IID种子510001—510200及新受控组合（沿用上一轮全因子规则、另换基础种子）。鲁棒位置误差模式作为额外工程模型独立测试，不混为题面噪声规则。
