# 预测、增益、重复合同与一月初始化补充

此目录全部资料由本地程序生成，原模型、数据和正式结果均未修改。原始只读依据为 `/Users/wuchenjie/Downloads/math-model-theory-59af4e0/题解/C题_跨日随机控制`；预测/control/data/selector 文件与原工作区版本逐一 SHA256 一致。本文档对应用户意见 6、7、8、11。

## 一键运行

```sh
python3 revision/prediction-control/audit.py all
python3 revision/prediction-control/gain_sensitivity.py
python3 revision/prediction-control/duplicate_lp_probe.py
python3 revision/prediction-control/verify_outputs.py
```

解释器为本机 Python 3.13，依赖 NumPy/SciPy/Matplotlib，HiGHS 从原项目 vendor 读取。所有结果保存在本目录，不覆盖正式334日。

## 预测

`forecast-errors.json` 给出1月15–31日四族开发误差、2–12月冻结预测误差、每个0/6/12/18点下一6小时的实时预测误差。后者每个目标时段恰好计数一次，包含原代码在线误差修正，不把重叠预报反复计数。日前与滚动预测的任务不同，不直接据二者数值决定采用新参数。价格采用浮动电价任务；固定任务电价给定，不应将它包装成预测成绩。

`forecast-metrics.tex` 可纳入正文，`forecast-vs-actual.pdf` 展示6月21日和9月23日，图中竖线为6/12/18点发布时刻。数据以kW绘制；模型 net 是10分钟电量，画图已乘6还原功率。对应CSV保存所有点，PNG已目视检查。

重要边界：1月开发分数是选择窗口的回顾性评分，不能宣称冻结选择器在1月25日就已可用。正式期使用1月结束前冻结选择器，每次只读合法历史。四个时点的未来实测值与未发布预报扰动测试，预测输出变化严格为0。未来信息测试只覆盖预测函数，不代替完整合同信息测试。

## 增益界限

`gain-summary.json` 是1月25–31日四问±10/±20，共8条七日闭环轨迹。首末6000，其他参数不变，仅通过LP函数包装器修改增益界限。原src未修改。`gain-*.json` 每次LP记录所有增益值、矩阵列索引，以及与目标、全部约束都完全无关的全零列。剔除标准是精确全零，不是主观认为“退化”。

±10下，问题二与问题四日前各768列中有388个结构零列且全部在边界，有效380列触边0；问题三与问题四日内各768列中有160个结构零列且全部在边界，有效608列触边0。±20同样有效列触边0。三情境费用保持不变，问题三节省14.471204729元（0.004232372%）。无证据支持因边界截断而付出实质费用，保留10。

`gain-trajectory-differences.json` 说明“费用相同”不等于轨迹逐元素相同。数值求解面对非严格凸目标及反馈动作并列最优可能返回不同轨迹；不能用14.47元反推边界放宽带来稳定收益。没有进行正式期重新选择参数。

`gain-diagnostic.tex` 为可引用表。原全年触边报告只有总触边数，没有保存每列数值，无法从该报告精确扣除全年零列；此处明确补的是开发窗口诊断，不能把0/380或0/608说成全年统计。

## 重复合同

`duplicate-contract-audit.json` 核验原固定年末6000输出：四对数值三对完全相等，一对差3.55e-15 kWh，均非抄表错误。`duplicate-lp-probe.json` 另在原保存库存处重构四个零点LP，所有当日合同与存档最大差为0。

源码的当日合同为每10分钟独立截距，当前日增益列因信息截断限制完全不参与当前合同，所以不能解释为“块内共享增益且截距为零”。为诊断值是否可任意打破，还约束原目标不超过最优值1e-7元，用二级LP分别最大、最小化两时段合同差；差异范围只在约1e-6至6.25e-5 kWh内。此结果表明这些相等值与实际数值优化一致，但不冒充唯一性解析证明或某一约束的确切致因。

## 一月初始化

`january-causal-greedy.json/.npz` 提供可执行替代方案：1月1日没有前一完整日，保留无电池动作、缺口紧急购电；1月2–31日只用前一天负荷与PV定每日合同，执行每日闭合贪心，首末库存每天均6000。使用题给固定日内电价规划同一合同，固定/波动任务各用对应真实价格事后结算。该公共初始化无需未来实际、不调用1月底选择器、不调用尚无成熟样本的场景生成器。

固定电价1月总费2,940,720.328630元，浮动电价3,353,793.169774元。2月1日库存仍6000，主334日所有策略与费用不变。此补充适合作为新初始化可执行实现，原src/run.py中的旧warmup保留为历史证据；如正文改用新初始化，应以本目录命令和NPZ为复现入口，不能声称原run.py直接生成新初始化。

## 验证

`verify_outputs.py` 不导入模型代码，直接从NPZ独立重算母线平衡、SOC、容量、功率、互斥、午夜、真正终点和两种电价费用，9条输出全部通过。`verification.json` 记录残差。这是独立公式的作者自检，不冒充外部独立审稿。
