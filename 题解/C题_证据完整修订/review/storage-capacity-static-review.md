# 容量新增分析静态一致性审查

reviewer_id: `/root/reviewer4_code`  
independent: true  
decision: changes_requested_for_reproduction_gate  
scope: 静态代码—文稿—图表映射；未重跑生成器，尚未对全部最终容量数字签署 PASS。

## 必修项

1. `revision/storage-study/verify_capacity.py:183`：结果缺失时默认分支输出 WAITING 后 `return`，进程退出码为 0。`reproduce.sh:33` 因此在 `set -e` 下继续运行。与此同时 `build_storage_evidence.py:14` 只检查已存在的 `capacity-audit.json` 的 PASS 字段；若目录中留有旧 PASS 报告，即有机会从过期报告构建表图。应将非等待模式的缺失状态改为非零退出，并在表图构建器中检验 audit 绑定的 12 份轨迹、指标 JSON 与 verifier 哈希均与当前文件一致。完整运行期结果的现有数值不因此判错，问题属于正式复现链未严格失败关闭。

## 静态通过项

- `capacity-section.tex:3–17` 的中心窗口、相同 6000 kWh 初末库存、相同功率和效率，与 `run_capacity.py` 的参数注入一致。W=4800 使用 161 节点，W=9600 使用 321 节点，步长均 30 kWh；W=0 使用 LockedInventory 单点停用分支。可行策略嵌套与精确期望费用下确界单调性证明成立，正文明确区分闭环近似实现的实际费用排序。
- `capacity-appendix.tex:2` 明确 9600 档使用原 334 日轨迹，新增八组为零/半窗口完整回放，原模型两日后缀只承担入口一致性检查；没有把两日后缀写成全年新实验。
- `zero_window_quantile.py:12–20` 在每日零时按同 count=7、两午夜视域、同固定/变动价权限重建场景并截取当天 144 段，与原 `src/run.py:57` 一致。价格加权 0.8 分位数、非负截断、目标差而非唯一合同差核验，与附录左右导数及最优条件相符。零窗口状态恒定，使当日逐段合同与未来合同增益不再发生目标耦合；此论断的范围被正确限定为相同有限场景规划。
- 当前零窗 JSON 每机制 48,096 段，最大绝对目标差 q2=3.6209257814334705e-10 元、q4_2=3.5936409403802827e-10 元；输出文本使用每段场景目标口径，没有与全年真实费用混用。
- `verify_capacity.py:103–111` 分解为原计划 p*q、增购 1.5p(r-q)+、退购净额 -0.5p(q-r)+、紧急 5pe，等价于 p[r+0.5|r-q|+5e]。表图构建器使用相应四项和 total，单位统一除 1e4 为万元。
- `verify_capacity.py:32–40,145–157` 用全部重叠非环绕 7 日块、48 块拼接截至 334 日、10,000 次重复；机制索引 0–3，比较索引中 0→9600 的 p=1、4800→9600 的 p=2，与附录声明的 SeedSequence 一致。表使用元/日日均区间，未把年度区间误填进日均列。
- 容量三点图横轴为可用宽度 0/4.8/9.6 MWh，纵轴为 334 日实际费用万元；左图 q2/q3、右图 q4_2/q4_3 分别配日前/日内图例。正文主动声明三点连线不外推一般边际收益关系。
- `main.tex:375` 和 `technical-appendix.tex:108` 分别引用容量正文/附录片段，片段进一步引用 storage-capacity PNG 与三张容量表、两个自动生成结果/核验片段和零分位文本，未发现引用到旧 free-terminal 文件。
- 新验证文本的功率按十分钟段上限 5000/6 kWh 核验，与 verifier 的能量容差定义一致；库存旧段落的 kW 容差已单独修正并审查通过。

## 最终数字绑定待办

待八组新全年全部完成和 capacity-audit/table 生成后，独立复算或绑定 12 组费用、两组对照区间、正负日、表格及正文舍入数字；检查新 audit 的来源哈希与稿内所有费用。此报告不覆盖 PDF 视觉排版；不覆盖此前已完成的工作簿全量复核。后续 final findings 应保留旧 findings 副本。
