# v2 边界与第一问独立阶段审查

审查人 `/root/independent_review`，独立审查；未修改计算主代码。范围限于 `run.py`、`control.py` 的 v2 边界、`check_terminal.py` 前缀复用和 `dispatch.py` 第一问经济目标；不验收全年候选排名、工作簿或最终交接。

证据：`review/check-v2-boundaries.py` 与 `review/v2-boundary-review.json`。基础检查耗时136.82秒，真实D1跨月补测耗时2.53秒，均退出0；本轮限定范围内的检查全部通过。这不是全年主项目的最终验收。

## 剩余时域与信息版本

`H<T` 时选择 `min(3,(T-H)/144)` 天辅助模型；由于两端均对齐午夜，该值是1、2或3，不会出现正时域被整除成0。`H=T` 时继续值直接为0且不读取模型文件。采用当前 as-of 所属月份的截止日，未按未来H月份选择模型。

以2月28日为根，截取3、4、5、8日实验的文件请求分别为截止日31的D1、D2、D3、D3；这是截获调用参数的路由测试，不冒充真实模型训练结果。真实末2日回放另验证8个发布节点均不加载尾值。显式传入7123 kWh后，初始库存保持7123，未被内部重设为6000。

限制：缩短辅助模型解决了模拟费用跨过真正年末的问题；辅助分布、时间粗化及有限迭代造成的近似仍存在。固定6000敏感性中，Markov下层的有限绝对值末罚仍为近似；实际终点由全局剩余步数可达集合硬性保证。

## 332日前缀复用

对目前限定的两日时域、线性末值 `affine_mpc` / `markov_mpc`，2月1日至12月29日的所有计划均满足H<T，故把 `final=None` 改成6000不会改变这部分LP/DP终端输入。固定末端的执行可达区间为 `[6000-N×750, 6000+N×(5000/6)/0.9]`；N≥7即包含全库存区间[1200,10800]。复用前缀中的最小N为288，因而该新增约束完全不生效。根据相同初态、相同预测和确定性控制可逐段归纳前缀相同。

12月30日午夜，旧日合同已全部履行，新日合同尚未签署；预测只取相同的外生历史，控制器也会重建计划。因此末2日重算所需的可控历史状态就是该午夜E，不需要携带已经清空的q/r。不能把此证明推广到任意长时域最优策略，或不同SDDP尾值版本之间的混合回放。

生产脚本已按审查建议增加断言：日期必须完整连续覆盖2025-02-01至12-31，日期索引31..364，候选仅上述两类线性末值控制器。因v1与v2的D1/D2可能改变12月30日前的SDDP决策，脚本现在明确拒绝直接套用本证明到SDDP候选。

独立使用Q2两个已完成候选的最后4日核验：自由末端复演与全年原轨迹逐值相同；固定/自由模式前2日逐值相同；继承原12月30日E后单独重算末2日，与4日固定末端回放的后缀逐值相同。q、r、充/放电、紧急电、弃电、SOC的最大差全部为0，固定末端均恰为6000。这些核验没有用于选择或评价全年最优候选。

## 第一问

独立显式q、充/放电、SOC和弃电LP给出经济目标 **35,126.948589289634元**；与更新后的 `dispatch.solve` 及重算 `q1.json/.npz` 最大差7.28×10⁻¹²元。原经济目标与求解器目标相符，未混入充放电正则。首末库存误差不超过6.37×10⁻¹² kWh，无紧急缺口，无同时充放，功率及SOC边界通过。

对1000组人为同时充放的动作，检验 `a=min(c,d/η²)`、`c'=c-a`、`d'=d-η²a`：库存变化保持，母线净供给增加 `(1-η²)a` 可免费弃置，至少一个动作变零；最大恒等式误差1.71×10⁻¹³。因此去除正则后，允许同时充放的LP仍可规范化成同经济费用的物理互斥实现。

真实D1模型就绪后已运行 `python3 review/check-v2-boundaries.py --month-only`，并核对此前通过的数值模块哈希不变。2月28日根节点的H为3月2日，仍采用截止日31、只含1个未来日的模型，随后H=T时不再读尾值。继承初值6432 kWh，三个午夜库存为6060.1506、8255.8285、1200 kWh，跨午夜跳变为0，最大功率5000 kW；未读取3月新版本或跨过真正评估末端。模型哈希、完整发布日志和误差均见JSON。

最终项目验收仍待主年度与交接全部冻结后开展。
