# 已登记实验的输出续接工具：有限范围审查

审查器：`/root/upgrade_code_review`。只涉及 `tools/finish_registered_outputs.py` 与隔离测试，不替代最终全项目审查。

初次审查用本地夹具确认两个缺陷：父工具被终止后，未继承文件描述符的子进程仍可继续写输出，而新进程可重新取得流水线锁；生产者截断状态 JSON 后死亡会停留在无限重试。另静态确认：原导出前只检查 complete 状态，没有重新检查当前模型证书及采用报告的证据绑定。

经主任务授权，本次直接修复这一个新工具：

- 各输出子进程通过 `pass_fds` 同时继承流水线锁与输出续接锁；工具意外退出时，仍运行的子进程维持两把锁。
- JSON 读取重试用单调时钟限制为 30 秒；如果已经获知生产者 PID，则损坏 JSON 重试期间也核验存活。短暂截断恢复后可继续正常执行。
- 取得流水线锁、重读 completed 之后，复用严格基线与正式验证 helper；验证完整当前模型证书；核对采用报告中三份验证证据的当前 SHA、冻结选择 SHA、唯一挑战/基线身份、经济门槛与采用身份一致性，以及四问题覆盖。全部通过才派发首个输出步骤。
- 原有子命令非零退出即中断、锁内重读生产者状态、拒绝并发续接、最终保留视觉与三人终审待办的行为保留。

执行 `python3 review/check_output_finisher.py`：**13 个隔离用例 PASS_WITHIN_SCOPE**。两个合法情况到达首个调用边界；损坏 JSON 超时、已知 PID 死亡、生产者失败、争锁前状态变化、重复续接、过期证书、过期采用证据、采用状态壳、正式轨迹变化及子步骤失败均按预期阻断。子步骤失败写入 failed 状态且没有继续派发。测试另启动一个无业务功能的替代子进程，终止它的临时父控制器后确认流水线锁仍不可取得。

测试中 baseline/formal helper 实际核验临时证据；仅模型证书鉴别器作明确替代，过期与当前分支均有测试。没有运行年度优化、真实导出、工作簿生成或 LaTeX，没有修改 src、冻结配置或正在计算的数据，没有生成最终评审结论。

工具源码 SHA-256：`fc8185f25e70ab54a9590116aac1684403adb01b2588d864fb60c2013e15ff20`。逐项证据见 `output-finisher-checks.json`。因为源码发生变化，实际导出前必须由生产流水线生成与当前版本匹配的模型证书和采用证据；不能沿用旧证书。
