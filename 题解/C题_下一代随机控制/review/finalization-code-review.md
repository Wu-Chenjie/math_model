# 完成门禁及论文来源审查

审查器：`/root/upgrade_code_review`，独立代码审查；2026-09-11。

本次仅静态读取生产工具，并使用 `check_finalization_guards.py` 在临时目录测试。没有运行优化或 LaTeX，没有写入最终评委/数学/代码终审记录，没有执行真实项目完成关闭。测试在请求第一份终审报告时强制停止；独立证书函数被显式隔离，因而结果仅证明其后的门禁缺口，不构成证书绕过或完整项目通过的声明。

## 当前阻断

1. **工作簿集合不完整仍可通过。** `finalize_project.py` 只检查 checks 长度为 5，可将同一条 result1 记录重复 5 次。应要求文件集合恰好为 result1、result2、result3、result4-2、result4-3，并每条唯一；除了 XLSX 本身，还应绑定读回使用的 payload、结果 NPZ/JSON 和导出选择记录，防止校验后源结果变化。
2. **未渲染全部页面仍可通过。** `rendered_pages={}`、`inspected_pages={}` 满足现比较；即使自动 QA 宣称一页也无阻断。应核对实际 PDF 页数、自动 QA 页号及渲染页一一对应，要求非空、无重复、每页哈希存在；视觉检查必须覆盖相同页。
3. **构建失败或缺少构建来源仍可通过。** `build.exit_code=1` 和 `source_hashes={}` 均可到达独立评审边界。应要求成功退出、精确的主 TEX 与生成片段绑定，并绑定图源、paper-results 与其生成记录。
4. **正式证据过期未被关闭工具重新检查。** 最终工具只看 formal/adoption 状态与冻结选择 SHA，未重查 formal 每个输出、独立验证子报告、采用报告 verification_gates 对应 SHA。验证后修改正式轨迹仍可到达评审边界。需复用采用工具的只读完整性检查，且覆盖整个登记的年度集合。论文记录只绑定其读过的部分轨迹，不能替代全候选检查。
5. **编译接受过期数值来源。** `build_paper.py` 仅读 paper-results.status，未比较其 source_hashes 或 paper-results-execution.generated。临时夹具故意提供错误源哈希，仍进入编译派发（已截停）。应在创建 PDF 前验证来源和生成片段；编译记录再绑定所有 TEX、实际引用图源和数值生成记录。

这些缺口由本地程序逐项确认，详情与被审源码 SHA 见 `finalization-guard-checks.json`。控制夹具会到达评审边界，所有变异也到达，故不是生产模型或优化问题；它们属于最终产物关闭的真实性问题。

## 其他静态观察

- `finalize_project.py` 对 baseline-execution.records 使用 all，没有验证非空/任务集合；对 execution-formal 只验状态。建议绑定当前基线比较报告、执行报告和登记完整性，避免状态壳被当成已执行。
- 应先完成所有存在性、源哈希与输出哈希检查，再写 findings/reproducibility/manifest。目前 findings 会先于输入哈希检查写成 pass；后续断言失败会留下部分“通过”产物。最后一次性提交这些派生记录。
- `build_paper_results.py` 正确检查冻结选择一致性、信息消融的 NPZ/JSON 及独立验证 SHA，并显式将未识别的因果下界保留为空。其主问题结果来自 comparisons.summaries；因此 comparisons 的源账单来源也必须通过统一当前证据检查，不能仅重新哈希一个可能已过期的汇总文件就称 verified。
- `build_paper.py` 的数学环境中文扫描、三遍 XeLaTeX、日志错误拦截、逐页渲染和自动 PDF 检查具有明确作用；上述审查不否认这些正常路径检查。

当前结论：**CHANGES_REQUESTED，仅针对最终产物门禁。** 不影响已独立检查的控制数学结论；最终年度、图文和三人终审仍待真实执行证据。

## 修复后复测

`current_evidence.py` 及 PDF/完成工具修订后，2026-09-11 执行 `python3 review/check_finalization_guards.py`：**26 个用例通过，PASS_WITHIN_SCOPE**。其中 2 个正例分别到达终审入口和首个编译调用入口；24 个反例均被拒绝，覆盖工作簿唯一集合、空渲染、TeX 绑定、正式 NPZ/JSON/数据/验证器/子报告过期、子检查失败、覆盖缺失及重复、编译退出失败、论文来源/生成片段/图源变化、PDF 页数和页序、未登记渲染文件。每个用例独立初始化临时证据，仅隔离模型证书函数；正式验证 helper 实际运行，页数由 pdfinfo 实际读取合法单页 PDF。

上节第 1—5 项的核心反例已修复。输入与预期输出的校验均已移到聚合 findings 写入之前，各输出文件使用临时文件替换，避免原先因验证断言失败留下提前 PASS 的问题。完整新源码 SHA 与逐项拒绝原因在 JSON 报告。

剩余静态边界：当前基线报告仍仅检查 status，baseline execution 的 all(records) 未要求非空/精确任务集合；工作簿证据仅绑定 XLSX，没有绑定读回源 payload/NPZ。工具测试的合法正例因此使用状态壳来隔离这些尚未加强的证据。这些边界已通知主任务，不将当前限定范围 PASS 解读为最终项目、全年度账单或三人终审通过。

## 基线及工作簿余项修复复测

新增 `require_baseline_current` 后，再次扩展临时夹具并运行：**41 项通过，PASS_WITHIN_SCOPE**。两个正例仍实际到达终审/编译入口；39 个反例均拒绝。新增反例覆盖仅 PASS 状态壳、空 cases、必要基线门禁遗漏、空证据、计数错误、比较器代码变化、冻结副本变化、复现源码变化、工作簿缺失来源、payload 变化、输出别名变化及 Q1 上游源变化。基线与正式 helper 均未隔离，只有独立模型证书函数继续隔离。

基线比较器 6 个原有微型回归测试亦通过。静态核对确认：正式结束后还需基线 execution 非空、全部 exit0 且覆盖指定脚本；此段位于三人终审之后，当前测试在终审入口硬停止，故没有通过伪造最终审稿记录执行它。实际基线全流水线完成、实际工作簿逐单元格读回、真实 PDF 视觉检查及三个终审均不在本临时夹具 PASS 范围内。

上轮列出的基线状态壳和工作簿源哈希余项已修复；截至本次有界复核，没有新增实际阻断。最终项目仍须等待真实证据，不据此关闭 manifest。
