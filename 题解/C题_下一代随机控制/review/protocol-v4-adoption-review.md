# Protocol v4 与采用器复核

审查人：`/root/upgrade_code_review`。本轮只编辑review文件，未启动真实优化。当前结论：**v4选型与M1采用的被测流程通过；整个项目仍须等待真实证据门禁。**

## v4匹配M1门槛

最终候选末值选择后，重新建立只有lower不同的final_matched_M0/final_matched_M1，并在1月用同末值比较。最终M1未通过则降回M0，随后再检查最终gain；两者均登记年度，避免后来按正式结果增补有利对照。正式采用器额外比较最终M1与该完全匹配M0，直接门槛不通过就不能凭相对旧模型的总体节省采用M1。

临时夹具完整调用采用器主流程，但不提供有效验证证书，因此所有夹具最终必须PENDING：

| 夹具 | 相对旧模型的整体节省 | M1相对匹配M0 | 合并经济门槛 |
|---|---:|---:|---|
| 其他模块掩盖M1负贡献 | 10% | -1.1235955056179776% | 拒绝 |
| M1本身有直接收益 | 10% | 5.2631578947368425% | 通过经济项，仍待证据 |
| M1没有贡献 | 10% | 0% | 拒绝 |

这些数值是人为设置的单元测试账本，不是新模型年度结果，不可进入竞赛结果表。机器证据见`adoption-matched-M1-checks.json`。

## 冻结、证书及异常

- v4重新运行`check_experiment_protocol.py`通过；错误protocol/integrity摘要、正式开始后重写开发、最终触边逃逸的v2反例继续被拒绝或正确扩界。
- 首次完整调用采用器发现numpy.bool_不能JSON序列化的真实运行阻断；已由主agent把checks统一转Python bool修正，主流程复测成功。原失败保留`adoption-serialization-failure.json`，不能把修复前主入口描述为已能完成运行。
- 证书生成器现在要求指定报告存在、明确PASS/PASS_WITHIN_SCOPE且independent=true。缺报告、空状态、失败状态即使子程序退出0仍拒绝。四种独立临时子程序夹具均通过。
- `certificate_is_current`核对完整绑定文件集合、完整测试记录、日志与独立子报告哈希。独立夹具进一步删除记录、修改子报告或日志，均被拒绝。
- 采用器现在核对formal报告是否覆盖冻结全部问题/配置、没有缺失或重复，记录全PASS且对应实际输出和独立报告哈希一致。空records的PASS壳在三个采用夹具中均不能通过。
- 主agent尚需确保用于比较的冻结旧年度轨迹文件本身仍匹配baseline-freeze哈希；仅检查baseline复现报告的status不能检测报告产生后的基线引用文件变更。此为最终证据绑定建议，未观察到真实基线被改动。

建议将本轮新增`check_adoption_matched_M1.py`和`check_certificate_guards.py`也纳入正式模型检查证书清单；否则证书尚未自动重跑本轮采用器反例。

本报告不代替基线完整复现、真实1月冻结输出、全部334日物理账单验证、因果模型审查或论文最终验收，也不声称新模型必然优于旧模型。
