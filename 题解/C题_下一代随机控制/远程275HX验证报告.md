# 275HX 远程验证报告

结论：SSH、固定 LP 数值验证、两套七日轨迹的独立物理与计费验证通过；跨平台逐项轨迹等价性未通过，暂不接入正式年度结果。

## 范围

使用相同源码与数据、一月25—31日、相同初末库存及冻结配置；远程运行 Python 原始参考后端，不使用未认证的 Windows C++ 后端。不调整模型或选择参数。NumPy 2.4.1、SciPy 1.17.1、HiGHS 1.15.1，Python 3.13 系列；操作系统、CPU 架构和底层二进制不同。

## 固定 LP

最大目标差 5.82076609135e-11；最大解分量差 6.23003870714e-11。全部原始约束误差小于 1e-5，四个输入文件哈希与本机一致。

八 LP 并发批次：本机 6.565215 秒，远程 6.519513 秒，包含进程启动。本测量未显示明显吞吐提升，不代表全年速度。

## 七日闭环

| 配置 | 费用差（元） | 最大库存差（kWh） | 物理与计费 | 轨迹完全一致 |
|---|---:|---:|---|---|
| 7a7d9f1f5f762810 | -5.82076609135e-11 | 60 | 通过 | 否 |
| c4458f2f6fb06e54 | 2.91038304567e-10 | 1.90290742974 | 通过 | 否 |

两套运行的合同差均小于 1e-9，但电池充放与弃电存在可见差异。差异可能涉及浮点运算及近等值动作选择，尚未完成逐时根因证明，不将其宣称为已证明的等价最优解。费用一致不能推出未来年度账单必然一致。

## 执行记录与后续门槛

远程目录：G:/math__model/题解/C_remote_verify。命令：venv/Scripts/python.exe remote_probe/benchmark.py --workers 8；venv/Scripts/python.exe closed_loop_verify.py（设置 PYTHONUTF8=1）。

固定 LP 命令正常退出。短闭环 SSH 会话出现 Broken pipe，重连后两套完整 NPZ、JSON 和 complete 进度文件均存在；已取回并用本机独立验证器逐项核验。不能将 SSH 断线误记为整个验证脚本正常退出。

正式分担计算前，需要定位有意义的动作差异并制定统一数值/择优规则；若修改规则，须两台机器共同重新验证和冻结，不能仅修补远程或覆盖原基线。目前不启动年度任务。

证据：artifacts/remote-validation/benchmark-comparison.json；closed-loop-audit.json；各原始 JSON、NPZ。验证脚本：remote_probe/closed_loop_verify.py 与 audit_remote_closed_loop.py。
