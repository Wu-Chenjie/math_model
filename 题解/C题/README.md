# C题建模计算交接包

先阅读[建模计算交接](建模计算交接.md)，再按需要查看[指定日期结果表](指定日期结果表.md)。本包不包含代写论文或论文PDF。

| 内容 | 位置 |
|---|---|
| 模型、推导、信息边界、参数来源、结果解释与局限 | `建模计算交接.md` |
| 问题1及四个指定日期的题面所需表格 | `指定日期结果表.md` |
| 5份完整XLSX、逐时策略与发布快照CSV | `计算结果/` |
| 可选用PNG和SVG图 | `figures/` |
| 生产代码与复现入口 | `src/`、`复现.sh` |
| 全精度数组、指标、对照组、压力测试、结果登记 | `artifacts/` |
| 独立reviewer审查与模拟评分 | `review/` |
| 不变原始输入副本与SHA-256 | `inputs/`、`modeling-manifest.json` |

时间轴已在输出副本中修正为00:00–24:00。问题2–4每天循环SOC=6000与退购退款方式均属于明确声明的建模口径。问题2–4交付的是经过验证的可行近似策略，不是完整随机调控问题的全局最优解。

复现：本机终端进入该目录执行`bash 复现.sh all`。详细解释器依赖及软件版本见交接文档第11节；普通计算依赖另列于`src/requirements-compute.txt`。重跑会更新生成结果，应对新哈希重新审查。
