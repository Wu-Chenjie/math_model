# 第三轮深化

保持两版冻结结果不变。先处理完整接收域与少点连续覆盖，最后做机制分层和真实费用比较，不再调整2-opt参数。

1. Q2：利用首次接收已知R≥r，把完整鲁棒域写成4圆盘交；独立推导全域minimax下界与上界，明确连续与量化模型以及地图裁剪限制。
2. Q4：搜索规则环与非格点布局，目标20—22点；连续证书验证每个空间单元，而不是只看随机方向测试。失败构造保留记录。
3. 广泛机制实验：固定源数10/13/16、定向占比、边界源占比、几何集中/共线与噪声相关，比较旧基线和冻结策略；数据生成规则独立记录，不能称分布无关平均性能。
4. 可证的分布无关部分：保留全清除和虚拟最坏预算；任何普适效率优势必须有证明，否则仅作分层实验结论。
5. 新布局冻结后使用新独立种子，输出交接文档、图表、源码、连续证书、独立评审与完整复现包。
